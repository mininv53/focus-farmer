# ASSETS.md — drop your own art and audio here

The MVP ships with placeholder **procedurally generated pixel art** (drawn at
runtime by `lib/game/textures/procedural.ts`) and **synthesized audio**
(generated by `lib/audio/synth.ts`). Replace either by dropping files into the
paths below — code will pick them up automatically once the loader hooks
described in this doc are wired up (TODOs marked below).

## Conventions

- All sprites are **pixel art**, integer-scaled (`image-rendering: pixelated`).
- Spirit sprites are **32×32 px** with a transparent background.
- Tiles are **32×32 px**, opaque, designed to tile seamlessly horizontally
  _and_ vertically.
- Audio is `.mp3`, mono, 44.1 kHz, **−14 LUFS** target loudness, less than
  500 ms for one-shots and looping for ambient.

## Sprites

Drop these PNGs into `public/sprites/spirits/`. To wire them up, add a `load.image`
call in a Phaser preload step that uses the same texture key as the procedural
fallback (e.g. `spirit-pip-sprout`) — the bundled file will win over the
procedural texture.

| Filename                                      | Size  | Description                                                  |
| --------------------------------------------- | ----- | ------------------------------------------------------------ |
| `public/sprites/spirits/pip-sprout.png`       | 32×32 | Pip the Sprout (common). Soft green moss-blob with two eyes. |
| `public/sprites/spirits/timothy-mole.png`     | 32×32 | Timothy the Mole (common). Earth-tone digger with feet.      |
| `public/sprites/spirits/lily-fairy.png`       | 32×32 | Lily the Fairy (common). Pink fairy with translucent wings.  |
| `public/sprites/spirits/echo-lantern.png`     | 32×32 | Echo the Lantern (rare). Floating teal crystal with glow.    |
| `public/sprites/spirits/onyx-crow.png`        | 32×32 | Onyx the Crow (rare). Indigo bird with shiny eyes.           |
| `public/sprites/spirits/mira-brook.png`       | 32×32 | Mira the Brook (rare). Aqua water-fairy with a calm face.    |
| `public/sprites/spirits/solstice-phoenix.png` | 32×32 | Solstice the Phoenix (legendary). Golden firebird.           |
| `public/sprites/spirits/aurora-dragon.png`    | 32×32 | Aurora the Dragon (legendary). Violet starlit dragon.        |

If you want **idle animation frames**, ship a horizontal strip:
`<id>-idle.png` at `4 frames × 32 px = 128×32 px`, and we'll add an animation
in `lib/game/scenes/realm-scene.ts`.

## Tiles

Drop into `public/tiles/`.

| Filename                      | Size  | Description                                              |
| ----------------------------- | ----- | -------------------------------------------------------- |
| `public/tiles/meadow.png`     | 32×32 | Seamless grass background tile (replaces `tile-meadow`). |
| `public/tiles/plot.png`       | 32×32 | Plot when occupied by a spirit (replaces `tile-plot`).   |
| `public/tiles/empty-plot.png` | 32×32 | Empty plot indicator (replaces `tile-empty`).            |

## Audio

Drop into `public/audio/`.

| Filename                       | Length | Description                                                                 |
| ------------------------------ | ------ | --------------------------------------------------------------------------- |
| `public/audio/start.mp3`       | <1s    | Soft chime when a session starts.                                           |
| `public/audio/complete.mp3`    | 1–2s   | Warm three-note jingle when a session completes.                            |
| `public/audio/summon.mp3`      | <1s    | Sparkle when a spirit appears.                                              |
| `public/audio/click.mp3`       | <0.2s  | Tiny UI click for buttons (subtle).                                         |
| `public/audio/ambient-day.mp3` | 30–90s | Looping cozy ambient: light wind, soft chimes, optional birds. Volume-safe. |

## Backgrounds (optional, post-MVP)

For richer parallax later:

| Filename                           | Size    | Description                |
| ---------------------------------- | ------- | -------------------------- |
| `public/sprites/bg/sky-day.png`    | 560×120 | Sky band, soft pastel.     |
| `public/sprites/bg/hills-far.png`  | 560×80  | Far hills (parallax 0.3).  |
| `public/sprites/bg/hills-near.png` | 560×100 | Near hills (parallax 0.6). |

## Open-source-friendly art packs (suggested seeds)

If you want **CC0** placeholders that are nicer than the procedural ones:

- [Kenney — Tiny Town](https://kenney.nl/assets/tiny-town)
- [Kenney — Cute Farm Crops](https://kenney.nl/assets/cute-farm-crops)
- [Pixel Frog — Pixel Adventure 1](https://pixelfrog-assets.itch.io/pixel-adventure-1)
- [Kenney — UI Audio](https://kenney.nl/assets/ui-audio)

These are all CC0 (public domain). When swapping in, rename the files to match
the table above.
